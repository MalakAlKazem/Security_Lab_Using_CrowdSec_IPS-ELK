events {}
http {
  server {
    listen 80;
    location / {
      root /usr/share/nginx/html;
      index index.html;
    }
    location /login {
      root /usr/share/nginx/html;
      try_files /login.html =404;
    }
  }
}
